# LLM Artifacts

This directory contains summaries or recaps generated by LLM tooling. It probably
doesn't include all of them, but when significant portions of code are generated
by tooling, it'd be good to capture what happened and why. Some of this shows up
in PR descriptions and comments, but this is another place to store longer term
and larger documents.
